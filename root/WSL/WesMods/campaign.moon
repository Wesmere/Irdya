wsl_handler
    id: "campaign"

  --scope: "WesMods"
  -- on_scan: (cfg, file, path) ->
  --   state.WesMods[cfg.id].loaded = false
  --   state.WesMods[cfg.id].path = path
  -- on_load: (cfg, file, path) ->
